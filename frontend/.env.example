# PostHog 产品分析 / 埋点
# 复制为 .env.local 并填入你的 PostHog project API key（.env.local 已被 gitignore）。
# 不配置时埋点自动禁用（no-op），不会发任何网络请求。

# PostHog project API key（公开 key，可打包进前端）
VITE_PUBLIC_POSTHOG_KEY=

# PostHog 实例地址：US 区填 https://us.i.posthog.com，EU 区填 https://eu.i.posthog.com
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ---- Supabase 认证（注册/登录）----
# 复制为 .env.local 并填入你的 Supabase 项目配置（都是公开 key，可打包进前端）。
# 两个都填了才启用登录；留空则不启用登录，直接进入应用（本地调试用）。
# 在 Supabase 控制台 Project Settings → API 里找：
#   Project URL      → VITE_SUPABASE_URL
#   Project API keys → anon public → VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
